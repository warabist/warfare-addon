export interface AutomaticGunData {
  itemId: string;
  ammoItemId: string;
  ammoProjectileId: string;
  capacity: number;
  rate: number;
  zoomRatio: number;
  uncertainty: number;
  reloadTime: number;
}
